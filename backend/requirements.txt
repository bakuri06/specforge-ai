fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.31
# Pinned exactly, not a range: a looser langgraph pin let pip resolve a
# langgraph/langchain-core/langgraph-checkpoint combination on a teammate's
# machine where interrupt()'s internal context lookup broke with
# "RuntimeError: Called get_config outside of a runnable context", even
# though the same code worked fine with these exact versions.
langgraph==0.2.76
langchain-core==0.3.86
langgraph-checkpoint==2.1.2
langgraph-sdk==0.1.74
httpx>=0.27,<0.28
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
pypdf>=5.0,<6
python-multipart>=0.0.12,<0.1
